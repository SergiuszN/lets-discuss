{% extends '@App/layout.html.twig' %}

{% block body %}
    {% include '@App/companyAdmin/components/navbar.html.twig' %}
    <ol class="breadcrumb">
        <li><a href="{{ path('app_company_admin_homepage') }}">Dashboard</a></li>
        <li><a href="{{ path('app_company_admin_worker_list', {'manager': app.user.id}) }}">Worker List</a></li>
        <li class="active">Worker Remove</li>
    </ol>

    <div class="page-header">
        <h1>Worker remove <br class="visible-xs"><small>Please confirm removal of this worker</small></h1>
    </div>

    <div class="remove-confirmation-area bg-danger">
        <div>Id: <b>{{ worker.id }}</b></div>
        <div>Name: <b>{{ worker.name }}</b></div>
        <div>Surname: <b>{{ worker.Surname }}</b></div>
    </div>
    {{ form(form) }}
{% endblock %}